{
  "_comment": "Per-game overrides applied on top of ../client-config-template.json when the release is staged. ONLY the keys that differ for this game belong here -- everything else, including the template's explanatory comments, comes from the template so there is one copy of it. dev-scripts/stage-release.ps1 applies these.",
  "_comment_interp": "175ms rather than the template's 250ms. Chosen by the user on 2026-08-28 after a render-knob sweep against meshghost-netsim's simulated bad link: 200ms was 'noticable unless i looked for it', 175ms 'didn't see anything at all', and 150ms 'living at the edge where its noticable sometimes'. TEVI is the ONLY adapter this has been measured for -- Pseudoregalia, Emerald and Crystal have never been netsim-tested for interp and keep the template's 250ms until they are. See agent_docs/verified.md and ADR 0040.",
  "interp": "175ms"
}
