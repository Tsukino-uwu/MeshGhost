-- Arms the Emerald drawn tier's per-frame motion trace (dev loader). A global outlives the script
-- that set it: move-trace-off.lua is how it stops.
MESHGHOST_EMERALD_MOVE_TRACE = true
console.log("MeshGhost: move trace ARMED -- probes/movetrace.log, one line per frame per peer")
