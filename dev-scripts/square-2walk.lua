-- Settings for probes/square_drive.lua: a 2x2 square, WALKING, corners flowing.
-- The walking counterpart of square-2run.lua -- a different step duration (16 frames, not 8) and
-- therefore a different test, since the ghost's rate comes from the peer's own gait.
MESHGHOST_SQUARE_SIDE = 2
MESHGHOST_SQUARE_RUN = false
MESHGHOST_SQUARE_STOPS = false
