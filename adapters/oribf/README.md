# Ori and the Blind Forest: Definitive Edition

**Status: candidate, not currently owned, not the current second-game pick.**

The brief originally named *Ori and the Will of the Wisps* as the second target game. This
folder is Blind Forest, a different (earlier) Ori title — the two should not be conflated.
Neither is currently owned. `TEVI` was picked as the actual second game (see
[adapters/tevi/README.md](../tevi/README.md)) because it's owned and Unity-based. Kept here
as a candidate for a later game slot if Ori is acquired.
