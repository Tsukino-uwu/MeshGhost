# Current status

**Active phases: 6 (TEVI) and 9 (Crystal), with 7 (Pseudoregalia), 10 (the Go side) and 11 (replays) live.** This file is an index of what
is open right now: **two lines per item, maximum** ([claude-md-cap.md](claude-md-cap.md)), **each carrying the date it was last re-checked, and an item dated
more than 2 days before this file's last commit fails preflight** — at this project's pace, 2026-08-31 is
already not current on 2026-09-02 (user's call). At the start of a session re-date what is still current and move the rest to `plans.md`,
`ideas.md`, the adapter's `UNVERIFIED.md` or `risks.md`; a quiet repo does not go red, because age is
measured against this file's own last commit. Records are never listed here — `verified.md`, the phase
files and each `VERIFIED.md` hold them. Why two lines and a date, not a total cap: [claude-md-cap.md](claude-md-cap.md).

## Open now

- 2026-09-10 - **TEVI: some bullet BIRTH rows never reach the watcher at all (seen as `spawned=False` flag updates for bullets that were never created).** Almost certainly the extras-cap trim dropping rows oldest-first while the receiver's `LastBulletSeq` advances past them, so the birth is skipped when a later copy arrives - shows up as missing shots under heavy fire, not overshoot; `tevi/UNVERIFIED.md`, the projectile entry.
- 2026-09-10 — **TEVI projectiles: the families that move themselves (Sable charged B's wave, Celia charged C's homing/acceleration) fly STRAIGHT on a ghost, and running the game's own `BulletBehave` on a dormant bullet is OFF FOR GOOD — it damaged the watcher live.** Owed: a design that never runs game code on a ghost (the shooter sending a small correction row only for bullets it sees deviate from their birth line) — `tevi/UNVERIFIED.md` ("flies ITSELF", the live addendum), `phases/phase6.md` 2026-09-10 (later).
- 2026-09-10 — **THE TEVI WIRE MUST BE BUILD-INDEPENDENT (user's call: cross-build play is a supported case, and it is also the only two-client rig): the two local installs are different game builds, and enum ordinals decoded as the wrong sprite ("white circles"). Bullet type/sprite go by NAME now; still ordinal: the follower-effect pool index (checked, with a same-family fallback), the two muzzle-flash pools, and whatever yesterday's orb/summon/shield rows carry** — audit list in `tevi/UNVERIFIED.md`; the fact in `phases/phase6.md`.
- 2026-09-09 — **config.json is LIVE on the client (Go side, mine, suite + race green, UNWATCHED in a game): a save is re-read within ~2 s and applied per key with a log line each; connection keys rejoin the relay; launch-time keys say so. `input_display.always` removed and both panels decoupled from `replay.indicator` (DLL `0a944434aa98`, unwatched).** Next: a live session where the user saves a chaser/hotkey change and reads the `config.json changed:` lines; TEVI's own config reads unaudited — `phases/phase10.md` 2026-09-09 (late), `docs/config.md`.

- 2026-09-09 — **THE DRIVEN GHOST'S HURT SIT IS SOLVED (cause user-confirmed on the Lua prototype; the DLL port `0f7a541e7d27` unwatched): the pawn's OWN `BP_HpHitable` ref, nulled by the decouple, restored at prepare. A level reload now re-prepares the pawn (the "reset last save" snapping). OPEN: the driven ghost never STANDS UP -- `EndInteract` ruled out; `stand_fn=` in `ghost_drive.txt` tries the next candidate per launch, signatures logged at prepare** -- `pseudoregalia/UNVERIFIED.md`, `phases/phase7.md` 2026-09-09 01:0x, `pitfalls/by-lesson.md` (two entries, one a crash of my own probe).

- 2026-09-08 — **The input track (ADR 0056) is built and green on the Go side, shipped OFF (`replay.inputs`): `input_sample` adapter→core, a second NDJSON track in `replay/inputs/`, ring always-on, correlated to the recording by a shared `recording_id`. Capture only — nothing plays one back.** **No adapter sends one. The Pseudoregalia census is DONE (2026-09-08, `probes/probe_inputcensus/`, verdict in `pseudoregalia/UNVERIFIED.md`): Enhanced Input's `GetBoundActionValue` is callable but its value needs C++ to read; the controller's `IsInputKeyDown`/analog reads with a hand-built `FKey` work end to end; the pawn's own fields are partial. **THE CAPTURE WORKS on Pseudoregalia (2026-09-08 13:46 run: the file reads back as the user's sequence, zero disagreements)**; the track carries the camera since the evening build (camera manager, not the controller), and the game-root config sits at `inputs` true / `record_on_launch` false with the ghost panel on; the drive toggle `ghost_drive.txt` (`mode=track`) is ARMED beside the main install's DLL and the user's two recordings sit in `replay/active/` (the sit clip) and `replay/staging/` (the glitch clip). NEXT: the stand-up check on the sit clip, the 1:1 side-by-side, the release.**
- 2026-09-08 — **The 23-agent adversarial review: 39 findings fixed on the Go side, ~109 still open in the untracked `REVIEW-FINDINGS.md` at the root (the checklist; delete it when it empties). Suite, `-race` and preflight green.** Highlights: `ValidateState` never bounded `Timestamp` (one gap, three separately-reported symptoms); the `tls_fingerprint` pin was bypassable two independent ways; `CloseGracefully` degraded to a reset for every non-TLS client AND a refused hello was not terminal, which were masking each other; the welcome cap was reopened at 21 members because it counted unescaped bytes — `phases/phase10.md`, the 2026-09-08 entry.
- 2026-09-08 — **The leak scanners could not see binaries, and preflight printed PASS on a leaking tree.** Fixed; `hygiene.yml` now runs the scan on every push with no path filter. **The four tracked, SHIPPED DLLs still carry the strings** — `UE4SS.dll` has the maintainer's username 86 times — and each needs a rebuild with the build directory stripped (preflight names the flag per file, every run). That rebuild is the user's call: `REVIEW-FINDINGS.md` D1.
- 2026-09-10 — **Both Archipelago-shaped proposals SHIPPED Go-side 2026-09-08 and the record said otherwise until today: `protocol_version` is a FLOOR (`AcceptsPeerVersion`, `MinProtocolVersion`) and `reject` carries a frozen `code` plus `retryable` (`RetryableForCode`).** What is still open is the adapter half — **all four still substring-match the prose and none reads `code`** (checked 2026-09-10); `core.isPermanentReject` reads the code first and falls back, so they work, they just do not benefit. ADR 0058, `contract.md`.
- 2026-09-08 — **netsim is milder than "bad wifi" and ADR 0046's 450 ms rests on it**: the loss model is memoryless Bernoulli, so the no-arg profile produces a 267 ms correlated gap about once every three hours and never exercises the 150-500 ms regime an interp buffer is sized for. Error direction is that 450 may be UNDER-sized. Needs a game and the user's eyes, not a code change — `REVIEW-FINDINGS.md` D5.

- 2026-09-09 — **The full-project stale-fact sweep: ~40 wrong assertions corrected across `_template/`, `contract.md`, `scaling.md`, the ADRs and the adapter docs; two new preflight checks (both proven to fail); one LIVE defect fixed (a second `game_id` on one core was accepted and retried forever)** — `phases/phase10.md`, the 2026-09-07 entry. (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 — **The Linux tester's stuttery ghosts were Nagle; `TCP_NODELAY` is CONFIRMED to have fixed it on Pseudoregalia** (1.3% of updates over 25 ms, nothing left in the 36-44 ms band, against 27-30% with a hard 40 ms floor) — still open on TEVI and both Lua adapters, plus a residual that is NOT Nagle: `pseudoregalia/VERIFIED.md`, `pseudoregalia/UNVERIFIED.md`. (re-dated 2026-09-09: still open, unchanged this session)


- 2026-09-09 — **DECLINED for now by the user, with its benchmark kept: batching a tick into one `render_frame` line (2.2x on the core's write side) would speed up what stopped being the constraint** — the live 512-chaser run had the bridge coping while the GAME ran at 5-7 fps. Revive it only if a game's adapter is behind while its framerate is healthy — `ideas.md`, the 2026-09-07 entry. (re-dated 2026-09-09: still open, unchanged this session)

- 2026-09-09 — **NOT fixed, filed with its measurement: the adapter's SEND rate sets what the core sends back.** The Pseudoregalia mod sends frames from UE4SS's thread at ~171/s while the game thread rendered 17 fps at 344 ghosts, so the core answered with ~59,000 render lines a second — `ideas.md`, the 2026-09-06 entry, with three approaches and what to measure first. (re-dated 2026-09-09: still open, unchanged this session)

- 2026-09-09 — **Code signing: the SignPath Foundation application (submitted 2026-09-06) was DECLINED 2026-09-09 on visibility grounds (stars, forks, contributors, independent mentions), reapplication invited; releases stay unsigned, false-positive reports per build are the lever** — `security-design.md`, the code signing entry (alternatives listed, none adopted); `docs/code-signing.md`.
- 2026-09-09 — **Crystal is ACTIVE (phase 9): goal is a second human on it and cross-patch play. Open before a stranger runs it: the `\uXXXX` decoder fix UNWATCHED; whether a ghost is solid on Crystal (it is on Emerald); a two-client Archipelago session from the release zip, not the dev tree; V1.1, Speedchoice v8.1 and AP-on-V1.1 ran in a five-build room 2026-09-09 (user: "now i can see all 5, on all 5 games"; per-build checks still open in `UNVERIFIED.md`); the age-out re-admission fix, the battle hold, the menu/text-box hide rule, surfing and the peer palette were all confirmed on screen the same day; **OPEN: a peer's Fly is dragged to the landing spot instead of flown** (`crystal/UNVERIFIED.md`, first suspect the out-of-play hold); per further patch, the addresses AND whether map ids still compare equal (`area_id` is equality-only in the core). No input track for Crystal: tile-stepped movement is already 1:1 from positions** — `phases/phase9.md` 2026-09-09, `emulator/pokemon/crystal/UNVERIFIED.md`.
- 2026-09-08 — **HIGH: v1.1.7 CRASHES at a new engine fault site (three dumps tonight, one tester's; the weapon-model apply is on one stack). Hardened DLL deployed and UNPROVEN; the crash watcher is armed. If it recurs, the feature ships OFF** — `pseudoregalia/UNVERIFIED.md`, the HIGH entry. (re-checked 2026-09-08: still open, unchanged -- no game session this pass)
- 2026-09-08 — **The client, config.json, log and replays moved to the GAME ROOT for TEVI and Pseudoregalia; the mods look nowhere else. Pseudoregalia CONFIRMED on screen; TEVI built and deployed to both installs, UNWATCHED** — `pseudoregalia/VERIFIED.md`, `pseudoregalia/UNVERIFIED.md`. (re-checked 2026-09-08: the TEVI half is still UNWATCHED)
- 2026-09-09 **The vanishing player SFX are FIXED: every ghost stole the player's audio attenuation listener (cause and Lua fix user-confirmed); the shipped C++ rewrites the call instead and is UNWATCHED** -- `pseudoregalia/VERIFIED.md`, `UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **Ghosts are AUDIBLE and should not be: confirmed by ear, parked as future work by the user's call -- silent by default with an opt-in setting; the silence clause covers one sound today** -- `ideas.md`, the SILENCE CLAUSE. (re-checked 2026-09-07: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **Pseudoregalia CONFIRMED on screen: a display name with quotes reaches the nametag whole (the JSON escape fix), and a zip of two recordings plays as two ghosts; the title-screen gate from the same session is still unwatched** — `pseudoregalia/VERIFIED.md`, `UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **v1.2.0 SHIPPED (2026-09-06 evening, from `ad28d8a4`; CI incl. race and fuzz green on the last Go-touching push, Docs green after four tree gates were cleared the same evening). The 2026-09-06 ghost-cost day for Pseudoregalia: the one-shot effect leak, the cheaper per-ghost tick, the registries, the spawn spike off, distance tiers, the outfit-aware nametag, modded swords actually applying; core: no chaser/replay cap; both exes carry a version resource. v1.1.7 (2026-09-05) was weapon-model sync and the relay's graceful rate-limit close. Still waiting on the user's eyes from 1.1.7: a 3s chaser LOOKING 3s behind (ADR 0049), a recording starting at the first real frame** — `pseudoregalia/UNVERIFIED.md` is the queue, newest first. (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **Emerald: a peer on a bike shows only when YOU are on one too — the spawned tier wears the LOCAL player's graphic, because the peer-graphic path is off pending a 32px OAM/subsprite fix; reads as intermittent, is deterministic** — `emerald/UNVERIFIED.md`, `emerald/FLAGS.md`. (re-checked 2026-09-06: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **Both Pokemon adapters' JSON decoders were fixed from measurement and are UNWATCHED: Emerald gained a depth cap (it followed 5000 levels), Crystal now decodes `\uXXXX` instead of substituting `?`** — each adapter's `UNVERIFIED.md`; `phases/phase8.md`, `phase9.md`. (re-checked 2026-09-06: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 — **All four adapter fuzzers now exist and are path-filtered per adapter.** Pseudoregalia's found that a whole-string key search is shadowable three ways (raw `orientation`, `prev`+omitempty, sorted map keys); fixed by scoped reads, plus ten unguarded narrowings bounded — `agent_docs/phases/phase7.md`, 2026-09-04. (re-checked 2026-09-07: unchanged, and it is a RECORD not an open item -- `phase7.md` holds it; droppable from here on the user's call) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-08 — Replay hotkeys: chords-only is the open half; the indicator half shipped 2026-09-05 (ADR 0052) — `ideas.md`. (re-checked 2026-09-08: still open, unchanged)
- 2026-09-09 — **FIXED, built and deployed, UNWATCHED: a ghost's sword is now mirrored from the peer's flag**, so a clip recorded before the pickup should show none — `adapters/pseudoregalia/UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 — **The FPS that outlived a ghost was its `BP_PlayerCam_C` camera rig (0.025 ms a frame each, cleared only by a level reload): FIXED in the DLL, watched clean by the census on all three despawn paths (peer, replay, chaser), UNWATCHED by the user in a session of their own** — `adapters/pseudoregalia/UNVERIFIED.md`. The 2026-09-04 "~2 Niagara per despawn" was the game's own churn. (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 — **Ghost cost, the whole day, all CONFIRMED on screen where a screen decides: adapter tick at 50 ghosts 11.7 -> 7.8 ms; a leak (one-shot effects never destroyed) FIXED; the five cadenced world walks replaced by event-fed object registries (local half 1.29 -> 0.16 ms); the per-spawn FixAllLights repair (18 of 19 ms) OFF and confirmed unnecessary; spawns spaced; distance tiers at the user's measured marks 6500 / 8500 / 10500 with the nametag kept when dormant; the nametag follows the outfit's height (confirmed, incl. a bogus-bounds outfit); modded swords reach ghosts (confirmed). 8 idle ghosts hold the 144 cap at zero-ghost p95.** UNWATCHED: a dormant tag FOLLOWING a moving peer; a moving real peer with the no-use parts off (step 2 measured at no gain, undecided); the `ls_rest` sub-slots at 50. NEXT (`ideas.md` 8-10): the loop seam as a teleport (core, ADR), `loop_pose_xf`'s slide call, the nametag's crouch/slide case — `adapters/pseudoregalia/VERIFIED.md` (four 2026-09-06 entries), `UNVERIFIED.md` (the night entry's list). A tester zip of the final build sits on the user's desktop. (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-08 — **The queued bitfield audit is DONE (read-only): 64 call sites, not 65 -- one of the greps was prose in a doc comment. `mg_read_bool` itself is CORRECT, verified against the submodule's mask and byte-offset arithmetic. **38 UNSAFE / 26 SAFE / 0 unknown**, with a premise correction: Blueprint-declared bools on this build are NOT packed (`VERIFIED.md:2418`), so the split is engine-property = unsafe, BP = safe. Seven UNSAFE sites are live in the shipped path every tick, and it is worse than reads -- `:14669` and `:23454` are byte WRITES that zero a whole engine flag byte. NOTHING IS FIXED: all of it is adapter-side C++ needing a game** -- the ranked list is `REVIEW-FINDINGS.md` section I7.
- 2026-09-08 — **An on-screen INPUT HISTORY, the user's design (two panels, player left / ghost right, each configurable): the player half is BUILT and SEEN in the user's screenshots (`cad2022f5083`, `input_display` section shipped off: `player ghost always background rows size unit count_side fps_note player_side ghost_side`), their words on the read-back still owed; the ghost half is BUILT on both sides (2026-09-08 evening, ADR 0057: `remote_input` streamed ahead on the render clock, reset behind every seam, hello-gated; Pseudoregalia draws it on a second panel) and SEEN by the user across seven restarts of their own recording, their 1:1 word owed; one empty panel on the pre-trace build is unexplained -- `pseudoregalia/UNVERIFIED.md`. The track carries the camera (camera manager, not the controller). **THE DRIVEN GHOST EXISTS as a dev rig (2026-09-09 night, `ghost_drive.txt` `mode=track`, `FLAGS.md`): jump, floor, swing, the recorded route walked and faced, the chair sit and the seated-walk glitch, all SEEN by the user; the hurt sit fixed (own health component) and the STAND-UP found 2026-09-09 midday (`change Move State(0)` + `customStopMontage(0.2)` on the stick's edge, user-confirmed on the Lua probe, the C++ copy unwatched); the afternoon on a real route (clings, Sunsetter, a pit fall): the wall push gated, the shadow arm, the corrector carrying velocity, the save's numbers copied -- all user-seen or measured -- and THE BOUND STICK (a hook answering the clone's `IA_Move` reads with the recorded stick) built UNWATCHED; open: the pit reset lands at the origin (`respawnTransform` never set on a clone), Power unmapped, the 1:1 side-by-side -- `pseudoregalia/UNVERIFIED.md`, `phases/phase7.md`.** Open beside it: why guard sits on so many rows, and one unattributed abort in a session with the Lua prototype loaded — `pseudoregalia/UNVERIFIED.md`.
- 2026-09-08 — **The recording indicator is a SCREEN-SPACE widget now (`REC_INDICATOR_SCREEN_SPACE`, `cad2022f5083`): the mechanism user-confirmed on the Lua prototype (stays drawn, no FOV drift, ShareX-aligned), the C++ port SEEN in the user's screenshot, their words owed** — `pseudoregalia/UNVERIFIED.md`, `VERIFIED.md` 2026-09-08.
- 2026-09-09 — Pseudoregalia's DLL was rebuilt with 42 reworked call sites and is **UNWATCHED**; nothing should look different, which is why it needs a look — `adapters/pseudoregalia/UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **The virtual clock is partly in: root, recorder flush and send-rate converted, `awaitTick` interruptible, preflight ratchet armed; the due-wait SLEEPS are still wall-clock pending an advance-until-quiescent helper** — `phases/phase10.md`, `ideas.md`. (re-checked 2026-09-06: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **Phase 11 follow-ons still unwatched: split times, anchors, adapter-side contact damage, an offline client mode, and replay/chaser on the other three adapters** — `phases/phase11.md`. (re-checked 2026-09-06: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-09 **Two shipped settings do nothing: `ghost_collision` reaches no adapter, nametags reach one of four. SEEN LIVE on Emerald in a two-client session — a spawned ghost is solid and blocks the player**; for that game the mechanism already exists (`freeGhostCollision`, behind a dev flag) and only the `session_policy` wiring is missing — `plans.md`, "Settings: defined once, honoured everywhere". (re-checked 2026-09-06: still open, unchanged) (re-dated 2026-09-09: still open, unchanged this session)
- 2026-09-10 (00:20) **Nothing is running** — relay, four Crystal cores (7779/7781-7783), the four BizHawk windows and the colour probe all down; ports 7777-7783 free; the four five-build loader targets point at the adapter only; rig setups: `running-the-rig.md`.

## Where the rest lives

What the user has not confirmed, per game: each adapter's `UNVERIFIED.md`, whose "This run" block is
what to watch next. Known gaps and assumptions: `risks.md`. Unscheduled ideas: `ideas.md`. The roadmap:
`plans.md`. The running log per phase: `phases/README.md`. Confirmed facts: `verified.md` and each
`VERIFIED.md`. Lessons: `checklists/`, filed via `pitfalls.md`.
