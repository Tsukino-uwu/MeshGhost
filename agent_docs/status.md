# Current status

**Active phase: 10 — the Go side, with 7 (Pseudoregalia) and 11 (replays) both live.** This file is an index of what
is open right now: **two lines per item, maximum** ([claude-md-cap.md](claude-md-cap.md)), **each carrying the date it was last re-checked, and an item dated
more than 2 days before this file's last commit fails preflight** — at this project's pace, 2026-08-31 is
already not current on 2026-09-02 (user's call). At the start of a session re-date what is still current and move the rest to `plans.md`,
`ideas.md`, the adapter's `UNVERIFIED.md` or `risks.md`; a quiet repo does not go red, because age is
measured against this file's own last commit. Records are never listed here — `verified.md`, the phase
files and each `VERIFIED.md` hold them. Why two lines and a date, not a total cap: [claude-md-cap.md](claude-md-cap.md).

## Open now

- 2026-09-08 — **The 23-agent adversarial review: 39 findings fixed on the Go side, ~109 still open in the untracked `REVIEW-FINDINGS.md` at the root (the checklist; delete it when it empties). Suite, `-race` and preflight green.** Highlights: `ValidateState` never bounded `Timestamp` (one gap, three separately-reported symptoms); the `tls_fingerprint` pin was bypassable two independent ways; `CloseGracefully` degraded to a reset for every non-TLS client AND a refused hello was not terminal, which were masking each other; the welcome cap was reopened at 21 members because it counted unescaped bytes — `phases/phase10.md`, the 2026-09-08 entry.
- 2026-09-08 — **The leak scanners could not see binaries, and preflight printed PASS on a leaking tree.** Fixed; `hygiene.yml` now runs the scan on every push with no path filter. **The four tracked, SHIPPED DLLs still carry the strings** — `UE4SS.dll` has the maintainer's username 86 times — and each needs a rebuild with the build directory stripped (preflight names the flag per file, every run). That rebuild is the user's call: `REVIEW-FINDINGS.md` D1.
- 2026-09-08 — **Two ADR-shaped proposals from comparing against Archipelago, deliberately not done: `protocol_version` should be a FLOOR not exact equality (we currently run AP's opt-in strict mode as our only mode), and a reject reason should be a machine-readable code not prose** — which is why all four adapters substring-match it and have the sense inverted: every PERMANENT refusal contains the word "relay" and only `"busy"` does not. `REVIEW-FINDINGS.md` N1/N2.
- 2026-09-08 — **netsim is milder than "bad wifi" and ADR 0046's 450 ms rests on it**: the loss model is memoryless Bernoulli, so the no-arg profile produces a 267 ms correlated gap about once every three hours and never exercises the 150-500 ms regime an interp buffer is sized for. Error direction is that 450 may be UNDER-sized. Needs a game and the user's eyes, not a code change — `REVIEW-FINDINGS.md` D5.

- 2026-09-07 — **The full-project stale-fact sweep: ~40 wrong assertions corrected across `_template/`, `contract.md`, `scaling.md`, the ADRs and the adapter docs; two new preflight checks (both proven to fail); one LIVE defect fixed (a second `game_id` on one core was accepted and retried forever)** — `phases/phase10.md`, the 2026-09-07 entry.
- 2026-09-07 — **The Linux tester's stuttery ghosts were Nagle; `TCP_NODELAY` is CONFIRMED to have fixed it on Pseudoregalia** (1.3% of updates over 25 ms, nothing left in the 36-44 ms band, against 27-30% with a hard 40 ms floor) — still open on TEVI and both Lua adapters, plus a residual that is NOT Nagle: `pseudoregalia/VERIFIED.md`, `pseudoregalia/UNVERIFIED.md`.

- 2026-09-06 — **FIXED and confirmed by me (Go side): a refused hello lost its `Reject` to a TCP reset, so a core retried a PERMANENT refusal forever.** CI's Linux race job found it; the 2026-09-05 graceful-close fix had gone into the rate-limit path only. All three handshake sites now close gracefully — `verified.md` 2026-09-06, `pitfalls/by-lesson.md`.

- 2026-09-06 — **FIXED on the Go side, mine to confirm and confirmed: a tester's "the client died" at ~343 ghosts was the core REFUSING ITS OWN GAME'S RECONNECT** (512 chasers; a write to a game that had stopped draining hit the 10s deadline, the socket closed, the game re-dialled 6 ms later and got "busy", so its mod started a second core). Three-part fix plus a regression test that fails 5/5 without it. **Its first version deadlocked** (a failed write inside `StartRecording` re-entered `StopRecording`); the everything-fuzzer found that 11.5 s into the first campaign after its peer space was widened past the roster cap at the user's ask, and the cleanup is now split by which locks it needs — `verified.md` 2026-09-06 (two entries), `pitfalls/by-lesson.md`.
- 2026-09-07 — **FIXED and confirmed by me (Go side): the ~350-ghost bridge ceiling was the core killing a SLOW adapter for being slow.** The frame path now enqueues and a writer goroutine coalesces superseded ghost positions; a 128-pack against a ~2 MB/s adapter survives with 270,872 stale renders discarded, where it was detached in 3s. Two ceilings, not one: the chaser pack also held 1.69 GB of private queues, now 7.20 MB shared — `verified.md` 2026-09-07, `phases/phase11.md`.
- 2026-09-07 — **DECLINED for now by the user, with its benchmark kept: batching a tick into one `render_frame` line (2.2x on the core's write side) would speed up what stopped being the constraint** — the live 512-chaser run had the bridge coping while the GAME ran at 5-7 fps. Revive it only if a game's adapter is behind while its framerate is healthy — `ideas.md`, the 2026-09-07 entry.

- 2026-09-06 — **NOT fixed, filed with its measurement: the adapter's SEND rate sets what the core sends back.** The Pseudoregalia mod sends frames from UE4SS's thread at ~171/s while the game thread rendered 17 fps at 344 ghosts, so the core answered with ~59,000 render lines a second — `ideas.md`, the 2026-09-06 entry, with three approaches and what to measure first.

- 2026-09-06 — **Code signing: the SignPath Foundation application was SUBMITTED 2026-09-06 (the user's form); waiting on their email. Policy page and Windows version resource shipped the same day. On approval: the user adds the API token as a repo secret and hands over the organization ID and project slug, then the signing step goes into `release.yml`** — `security-design.md`, the code signing entry; `docs/code-signing.md`.
- 2026-09-08 — **HIGH: v1.1.7 CRASHES at a new engine fault site (three dumps tonight, one tester's; the weapon-model apply is on one stack). Hardened DLL deployed and UNPROVEN; the crash watcher is armed. If it recurs, the feature ships OFF** — `pseudoregalia/UNVERIFIED.md`, the HIGH entry. (re-checked 2026-09-08: still open, unchanged -- no game session this pass)
- 2026-09-08 — **The client, config.json, log and replays moved to the GAME ROOT for TEVI and Pseudoregalia; the mods look nowhere else. Pseudoregalia CONFIRMED on screen; TEVI built and deployed to both installs, UNWATCHED** — `pseudoregalia/VERIFIED.md`, `pseudoregalia/UNVERIFIED.md`. (re-checked 2026-09-08: the TEVI half is still UNWATCHED)
- 2026-09-07 **The vanishing player SFX are FIXED: every ghost stole the player's audio attenuation listener (cause and Lua fix user-confirmed); the shipped C++ rewrites the call instead and is UNWATCHED** -- `pseudoregalia/VERIFIED.md`, `UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged)
- 2026-09-07 **Ghosts are AUDIBLE and should not be: confirmed by ear, parked as future work by the user's call -- silent by default with an opt-in setting; the silence clause covers one sound today** -- `ideas.md`, the SILENCE CLAUSE. (re-checked 2026-09-07: still open, unchanged)
- 2026-09-07 **Pseudoregalia CONFIRMED on screen: a display name with quotes reaches the nametag whole (the JSON escape fix), and a zip of two recordings plays as two ghosts; the title-screen gate from the same session is still unwatched** — `pseudoregalia/VERIFIED.md`, `UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged)
- 2026-09-06 **v1.2.0 SHIPPED (2026-09-06 evening, from `ad28d8a4`; CI incl. race and fuzz green on the last Go-touching push, Docs green after four tree gates were cleared the same evening). The 2026-09-06 ghost-cost day for Pseudoregalia: the one-shot effect leak, the cheaper per-ghost tick, the registries, the spawn spike off, distance tiers, the outfit-aware nametag, modded swords actually applying; core: no chaser/replay cap; both exes carry a version resource. v1.1.7 (2026-09-05) was weapon-model sync and the relay's graceful rate-limit close. Still waiting on the user's eyes from 1.1.7: a 3s chaser LOOKING 3s behind (ADR 0049), a recording starting at the first real frame** — `pseudoregalia/UNVERIFIED.md` is the queue, newest first.
- 2026-09-06 **Emerald: a peer on a bike shows only when YOU are on one too — the spawned tier wears the LOCAL player's graphic, because the peer-graphic path is off pending a 32px OAM/subsprite fix; reads as intermittent, is deterministic** — `emerald/UNVERIFIED.md`, `emerald/FLAGS.md`. (re-checked 2026-09-06: still open, unchanged)
- 2026-09-06 **Both Pokemon adapters' JSON decoders were fixed from measurement and are UNWATCHED: Emerald gained a depth cap (it followed 5000 levels), Crystal now decodes `\uXXXX` instead of substituting `?`** — each adapter's `UNVERIFIED.md`; `phases/phase8.md`, `phase9.md`. (re-checked 2026-09-06: still open, unchanged)
- 2026-09-07 — **All four adapter fuzzers now exist and are path-filtered per adapter.** Pseudoregalia's found that a whole-string key search is shadowable three ways (raw `orientation`, `prev`+omitempty, sorted map keys); fixed by scoped reads, plus ten unguarded narrowings bounded — `agent_docs/phases/phase7.md`, 2026-09-04. (re-checked 2026-09-07: unchanged, and it is a RECORD not an open item -- `phase7.md` holds it; droppable from here on the user's call)
- 2026-09-08 — Replay hotkeys: chords-only is the open half; the indicator half shipped 2026-09-05 (ADR 0052) — `ideas.md`. (re-checked 2026-09-08: still open, unchanged)
- 2026-09-07 — **FIXED, built and deployed, UNWATCHED: a ghost's sword is now mirrored from the peer's flag**, so a clip recorded before the pickup should show none — `adapters/pseudoregalia/UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged)
- 2026-09-06 — **The FPS that outlived a ghost was its `BP_PlayerCam_C` camera rig (0.025 ms a frame each, cleared only by a level reload): FIXED in the DLL, watched clean by the census on all three despawn paths (peer, replay, chaser), UNWATCHED by the user in a session of their own** — `adapters/pseudoregalia/UNVERIFIED.md`. The 2026-09-04 "~2 Niagara per despawn" was the game's own churn.
- 2026-09-06 — **Ghost cost, the whole day, all CONFIRMED on screen where a screen decides: adapter tick at 50 ghosts 11.7 -> 7.8 ms; a leak (one-shot effects never destroyed) FIXED; the five cadenced world walks replaced by event-fed object registries (local half 1.29 -> 0.16 ms); the per-spawn FixAllLights repair (18 of 19 ms) OFF and confirmed unnecessary; spawns spaced; distance tiers at the user's measured marks 6500 / 8500 / 10500 with the nametag kept when dormant; the nametag follows the outfit's height (confirmed, incl. a bogus-bounds outfit); modded swords reach ghosts (confirmed). 8 idle ghosts hold the 144 cap at zero-ghost p95.** UNWATCHED: a dormant tag FOLLOWING a moving peer; a moving real peer with the no-use parts off (step 2 measured at no gain, undecided); the `ls_rest` sub-slots at 50. NEXT (`ideas.md` 8-10): the loop seam as a teleport (core, ADR), `loop_pose_xf`'s slide call, the nametag's crouch/slide case — `adapters/pseudoregalia/VERIFIED.md` (four 2026-09-06 entries), `UNVERIFIED.md` (the night entry's list). A tester zip of the final build sits on the user's desktop.
- 2026-09-08 — **The queued bitfield audit is DONE (read-only): 64 call sites, not 65 -- one of the greps was prose in a doc comment. `mg_read_bool` itself is CORRECT, verified against the submodule's mask and byte-offset arithmetic. **38 UNSAFE / 26 SAFE / 0 unknown**, with a premise correction: Blueprint-declared bools on this build are NOT packed (`VERIFIED.md:2418`), so the split is engine-property = unsafe, BP = safe. Seven UNSAFE sites are live in the shipped path every tick, and it is worse than reads -- `:14669` and `:23454` are byte WRITES that zero a whole engine flag byte. NOTHING IS FIXED: all of it is adapter-side C++ needing a game** -- the ranked list is `REVIEW-FINDINGS.md` section I7.
- 2026-09-07 — Pseudoregalia's DLL was rebuilt with 42 reworked call sites and is **UNWATCHED**; nothing should look different, which is why it needs a look — `adapters/pseudoregalia/UNVERIFIED.md`. (re-checked 2026-09-07: still open, unchanged)
- 2026-09-06 **The virtual clock is partly in: root, recorder flush and send-rate converted, `awaitTick` interruptible, preflight ratchet armed; the due-wait SLEEPS are still wall-clock pending an advance-until-quiescent helper** — `phases/phase10.md`, `ideas.md`. (re-checked 2026-09-06: still open, unchanged)
- 2026-09-06 **Phase 11 follow-ons still unwatched: split times, anchors, adapter-side contact damage, an offline client mode, and replay/chaser on the other three adapters** — `phases/phase11.md`. (re-checked 2026-09-06: still open, unchanged)
- 2026-09-06 **Two shipped settings do nothing: `ghost_collision` reaches no adapter, nametags reach one of four. SEEN LIVE on Emerald in a two-client session — a spawned ghost is solid and blocks the player**; for that game the mechanism already exists (`freeGhostCollision`, behind a dev flag) and only the `session_policy` wiring is missing — `plans.md`, "Settings: defined once, honoured everywhere". (re-checked 2026-09-06: still open, unchanged)
- 2026-09-06 (night) **Nothing is running** — relay, fake peers, probes and the games all down at the end of the ghost-cost day, ports 7777-7790 free; both installs carry the final DLL (`43338c187518`), the scratch slot holds its stub, no dev toggle sits beside either DLL; `replay/active/` in the main install still holds the user's zip and six looping test clips (theirs to keep or clear); rig setups: `running-the-rig.md`.

## Where the rest lives

What the user has not confirmed, per game: each adapter's `UNVERIFIED.md`, whose "This run" block is
what to watch next. Known gaps and assumptions: `risks.md`. Unscheduled ideas: `ideas.md`. The roadmap:
`plans.md`. The running log per phase: `phases/README.md`. Confirmed facts: `verified.md` and each
`VERIFIED.md`. Lessons: `checklists/`, filed via `pitfalls.md`.
